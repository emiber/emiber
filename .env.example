# Copy this file to .env and paste your classic PAT (scopes: repo + read:user).
# .env is gitignored — never commit it. Used only for local runs of today.py;
# GitHub Actions uses the ACCESS_TOKEN repo secret instead.
ACCESS_TOKEN=ghp_your_token_here
